# Large pretrained embedding file (171 MB, over GitHub's 100 MB limit).
# Download separately, see README for the link.
glove.6B.50d.txt

# BERT checkpoint (438 MB). Reproducible by re-running the BERT training cells
# in notebooks/04_gru_and_bert.ipynb.
results/zawad/bert/checkpoint_best/
**/checkpoint_best/
*.safetensors

# Trained model weights (~99 MB total). Reproducible by re-running the
# corresponding training notebook.
*.weights.h5

# Frozen derived artifacts regenerated by notebooks/01_phase0_data_and_representations.ipynb
splits/*.parquet
representations/

# Python cache
__pycache__/
*.pyc

# Jupyter
.ipynb_checkpoints/

# OS / editor noise
.DS_Store
Thumbs.db

# Environments
.env
venv/
*.egg-info/
